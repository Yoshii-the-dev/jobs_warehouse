name: run_etl

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: build and run dc
        run: docker-compose -f JobsScraper/docker-compose.yml up postgresdb
        #docker compose wait SERVICE [SERVICE...] [OPTIONS]
